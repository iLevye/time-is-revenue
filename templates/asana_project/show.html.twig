{% extends 'base.html.twig' %}

{% block title %}AsanaProject{% endblock %}

{% block body %}
    <h1>AsanaProject</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ asana_project.id }}</td>
            </tr>
            <tr>
                <th>AsanaId</th>
                <td>{{ asana_project.asanaId }}</td>
            </tr>
            <tr>
                <th>IsPinned</th>
                <td>{{ asana_project.isPinned ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('asana_project_index') }}">back to list</a>

    <a href="{{ path('asana_project_edit', {'id': asana_project.id}) }}">edit</a>

    {{ include('asana_project/_delete_form.html.twig') }}
{% endblock %}
